# Copy to .env or .env.local and fill in. Do not commit .env or .env.local.

# Discord bot (required for discord_bot.py)
DISCORD_BOT_TOKEN=

# Only needed if you want compound review to run at 10:30 PM when Cursor is closed.
# In Cursor, "run compound review" / "run auto-compound" use Cursor's AI — no key needed.
# ANTHROPIC_API_KEY=
# AGENT_CMD=amp execute

# Pokémon TCG API (optional). Improves rate limits for scripts/seed_db.py.
# POKEMON_TCG_API_KEY=

# Skip API and seed DB from fallback only: SKIP_POKEMON_API=1

# Price alerts (Discord bot)
# How often to check alerts (seconds). Default: 300 (5 minutes)
# ALERT_CHECK_INTERVAL_SECONDS=300
#
# Use live TCGPlayer market prices from PokemonTCG API when evaluating alerts.
# Requires network access; recommended to set POKEMON_TCG_API_KEY to avoid rate limits.
# ALERTS_USE_LIVE_PRICE=1
#
# Live price cache TTL (seconds) for PokemonTCG API calls. Default: 60
# LIVE_PRICE_CACHE_TTL_SECONDS=60
